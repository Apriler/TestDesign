<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<contextName>bdd(BDBASIC)</contextName>
	
	<!-- 控制台本地代码调试时用于root打印 -->
	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-4relative [%thread] %-5level %logger{35} - %msg %n
			</pattern>
			<charset>UTF-8</charset>
		</encoder>
	</appender> 
	
	<!-- RollingFileAppender：滚动记录文件，先将日志记录到指定文件，当符合某个条件时，将日志记录到其他文件 -->
    <!-- 以下的大概意思是：1.先按日期存日志，日期变了，将前一天的日志文件名重命名为XXX%日期%索引，新的日志仍然是demo.log -->
    <!--             2.如果日期没有发生变化，但是当前日志的文件大小超过1KB时，对当前日志进行分割 重命名-->
	<appender name="MY-DEFIEND" class="ch.qos.logback.core.rolling.RollingFileAppender">    
        <File>${BDHOME}/logs/bdd/bdds.log</File>
        <!-- rollingPolicy:当发生滚动时，决定 RollingFileAppender 的行为，涉及文件移动和重命名。 -->
        <!-- TimeBasedRollingPolicy： 最常用的滚动策略，它根据时间来制定滚动策略，既负责滚动也负责出发滚动 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">    
            <!-- 活动文件的名字会根据fileNamePattern的值，每隔一段时间改变一次 -->
            <!-- 文件名：log/bdb-log.2017-12-05.0.log -->
            <fileNamePattern>${BDHOME}/logs/bdd/bdds-log.%d.%i.log</fileNamePattern>
            <!-- 每产生一个日志文件，该日志文件的保存期限为30天 --> 
            <maxHistory>10</maxHistory>   
            <timeBasedFileNamingAndTriggeringPolicy  class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">    
                <!-- maxFileSize:这是活动文件的大小，默认值是10MB，可改成KB -->  
                <maxFileSize>300MB</maxFileSize>    
            </timeBasedFileNamingAndTriggeringPolicy>    
        </rollingPolicy>    
        <encoder>    
            <!-- pattern节点，用来设置日志的输入格式 -->
            <pattern>    
                %d %p (%file:%line\)- %m%n  
            </pattern>    
            <!-- 记录日志的编码:此处设置字符集 - -->
            <charset>UTF-8</charset>
        </encoder>    
    </appender>
    
    <!-- 1. 输出SQL 到控制台和文件-->
	 <logger name="org.hibernate.SQL" additivity="false" >
	    <level value="ERROR" />
	    <appender-ref ref="STDOUT" />
	    <appender-ref ref="MY-DEFIEND" />
	</logger>

   <!-- 2. 输出SQL 的参数到控制台和文件-->
	<logger name="org.hibernate.type.descriptor.sql.BasicBinder" additivity="false" level="ERROR" >
	    <level value="ERROR" />
	    <appender-ref ref="STDOUT" />
	    <appender-ref ref="MY-DEFIEND" />
	</logger>
    
    <!-- 总体设置日志级别：级别依次为【从高到低】：FATAL > ERROR > WARN > INFO > DEBUG > TRACE  -->
	<!-- 部署时改成 MY-DEFIEND -->
	<root level="INFO">
		<appender-ref ref="STDOUT" />
        <appender-ref ref="MY-DEFIEND" />
	</root>
	<!--  特殊自定义包设置日志级别 -->
	<!-- <logger name="com.bonc.generalplatform.basic.mapper.sysmanagement" level="INFO">    
        <appender-ref ref="STDOUT" />    
    </logger>   -->
</configuration>